if age >= 50: #Filters out Agents that are too young to die of old age
            if age >= 50 and age <= 60:
                if random.randint(0, 100) < 10: #This should be a 10% chance of dying
                    self.death(population)
            elif age >= 61 and age <= 70:
                if random.randint(0, 100) < 30: #This should be a 30% chance of dying
                    self.death(population)
            elif age >= 71:
                if random.randint(0, 100) < 50: #This should be a 50% chance of dying
                    self.death(population)

class house(): #Houses formed by Nobles; Sub-unit of Dynasties
    def __init__(self, house_id, model):
        house_id = len(houseList)-1
        self.house_id = house_id
        self.house_name = ""
        self.founding_house = []
        self.parent_house = []
        self.cadet_houses = []
        self.founder = []
        self.house_members = []
        self.house_head = self.founder
        self.exstinct = False
    def new_house_head(self):
        ohh = self.house_head #ohh = _O_ld _H_ouse _H_ead
        nhh = [] #nhh = _N_ew _H_ouse _H_ead
        potential_hh = ohh.children
        for i in range(len(potential_hh)-1):
            if i.alive == True:
                if i.sex == "Male":
                    nnh = i
                else:
                    pass
            else:
                pass
        self.house_head = nnh
    def house_exstinction():
        pass
    def house_founding(self):
        founder = self.founder
        if random.randint(0, 100) < 20:
            self.house_name = founder.patronym
        else:
            self.house_name = random.choice(nameListL)
        founder.lastname = self.house_name
